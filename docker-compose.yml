services:
  gptr-mcp:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MCP_TRANSPORT=sse
      - DOCKER_CONTAINER=true
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - TAVILY_API_KEY=${TAVILY_API_KEY}
      - PYTHONUNBUFFERED=1
    env_file:
      - .env
    volumes:
      # Mount logs directory for persistent logging
      - ./logs:/app/logs
    restart: unless-stopped
    extra_hosts:
    - "host.docker.internal:host-gateway"

  mcpo:
    image: ghcr.io/open-webui/mcpo:main
    container_name: mcpo
    ports:
      - "9000:9000"
    volumes:
      - ./mcpo_config.json:/app/config.json
    restart: unless-stopped
    command: >
      mcpo --config /app/config.json --port 9000 --hot-reload
    depends_on:
      - gptr-mcp

  # Optional: Add a reverse proxy for production
  # nginx:
  #   image: nginx:alpine
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #   depends_on:
  #     - gptr-mcp
  #   restart: unless-stopped
  #   profiles:
  #     - production

networks:
  gptr-mcp-net:
    driver: bridge
    # To connect to existing n8n network, run:
    # docker network connect n8n-mcp-net gptr-mcp 